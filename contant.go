package gowk

var (
	AUID          = "auid"
	UID           = "uid"
	APPKEY        = "AppKey"
	AUTHORIZATION = "Authorization"
)

// 分隔符
var (
	SEP1 = string([]byte{0x01})
	SEP2 = string([]byte{0x02})
)
